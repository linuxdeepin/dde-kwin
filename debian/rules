#!/usr/bin/make -f

export QT_SELECT=5

%:
	dh $@

override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info

override_dh_auto_test:
	:

# only deepin or uos enable built-in blur plugin
override_dh_auto_configure:
	dh_auto_configure -- \
	-DENABLE_BUILTIN_BLUR=ON
